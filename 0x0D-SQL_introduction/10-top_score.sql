-- script to list the score,name from the second_table in order of descinding
-- results should display bothe the score and the name in desc order

SELECT score, name FROM second_table ORDER BY score DESC;
